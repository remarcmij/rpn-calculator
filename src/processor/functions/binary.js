export default {
  add: (x, y) => y + x,
  sub: (x, y) => y - x,
  mul: (x, y) => y * x,
  div: (x, y) => y / x,
  pow: (x, y) => Math.pow(y, x),
  sqrt: Math.sqrt,
  sin: Math.sin,
  cos: Math.cos,
  tan: Math.tan,
  reciprocal: (x) => 1 / x,
};
